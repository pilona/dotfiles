# vim: ft=zsh

unset MAILCHECK
unset HISTFILE
HISTSIZE=4096

set -o vi
set -o nolog
set +o notify

case "$(cat /proc/$$/comm)" in
    zsh)  . ~/.profile.d/zsh  ;;
    bash) . ~/.profile.d/bash ;;
    mksh) . ~/.profile.d/mksh ;;
esac

. ~/.profile.d/prompt
. ~/.profile.d/venv
